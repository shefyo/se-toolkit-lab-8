# `docker-compose.yml`

## What is `docker-compose.yml`

File: [`docker-compose.yml`](../docker-compose.yml).

<!-- TODO explain everything about docker-compose.yml -->
<!-- Link to this doc when mentioning docker-compose.yml -->

### Services

See [`Docker Compose` services](./docker-compose.md#service)

#### `app` service

#### `postgres` service

#### `pgadmin` service

#### `caddy` service

### Volumes
